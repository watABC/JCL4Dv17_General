  //JCL_all_delete
  //20150412 yabe

C_LONGINT($i)
C_POINTER($tablePrt)

For ($i;1;Get last table number)
	
	If (Is table number valid($i)=True)
		
		  // 20150408 wat 郵便番号テーブルは除く
		If (Table name($i)#"Z_PostalCode")
			
			$tablePrt:=Table($i)
			READ WRITE($tablePrt->)
			ALL RECORDS($tablePrt->)
			DELETE SELECTION($tablePrt->)
			UNLOAD RECORD($tablePrt->)
			READ ONLY($tablePrt->)
			
		End if 
		
	End if 
	
End for 
