  //JCL_fld_numOfFields
  //20150412 yabe wat
  //フィールド数を求める

C_LONGINT($1;$i)
$i:=$1
C_LONGINT($2;$k)
$k:=$2
C_LONGINT($numOfFlds;$0)

For ($k;1;Get last field number($i))
	
	If (Is field number valid($i;$k)=True)
		
		$numOfFlds:=$numOfFlds+1
		
	End if 
	
End for 


$0:=$numOfFlds