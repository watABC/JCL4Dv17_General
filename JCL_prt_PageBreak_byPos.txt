  //JCL_prt_PageBreak_byPos  //20191213 wat  //印刷可能領域に収まるかを判断、収まらなければ改ページC_LONGINT($1;$inPos)$inPos:=$1C_LONGINT($2;$numOfPages)$numOfPages:=$2  //総ページ数C_LONGINT($3;$section_height)$section_height:=$3C_POINTER($4)  //ページ番号を返すC_LONGINT($nombre)  //ページ番号$nombre:=$4->C_LONGINT($0;$result_feed_pos)  //現在の印刷位置から、次のセクションが印刷可能領域に収まるかを判断、収まらなければゼロを返す$result_feed_pos:=JCL_prt_GetNextPos ($inPos;$section_height)If ($result_feed_pos=0)		  //ページに残りの印刷領域がない場合は　改ページしてヘッダを印刷	JCL_prt_PageBreak ($nombre;$numOfPages)	$nombre:=$nombre+1	End if $4->:=$nombre$0:=$result_feed_pos