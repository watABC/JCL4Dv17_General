  //JCL_prt_PrintSettings  //20200115 wat  //プリント設定を保持する仕組みC_TEXT($1;$key)$key:=$1C_LONGINT($0)C_BLOB($blob)  //アプリに保存された印刷設定をロード$blob:=KV_BLOB_VALUE ($key)$err:=BLOB to print settings($blob)  //エラーがなければ、ユーザにダイアログ表示、印刷設定が変化するPRINT SETTINGS(0)If (OK=1)	  //変化した現在の印刷設定を保存	$err:=Print settings to BLOB($blob)	KV_BLOB_VALUE_save ($key;$blob)	End if If ($err=-1)	ALERT("印刷設定を保存する際にエラーが発生しました。"+String($err))	End if $0:=OK